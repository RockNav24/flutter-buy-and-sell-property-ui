import 'package:flutter/material.dart';

// Top - Name section
const double c_logoSize = 0.05;
const double c_logoLetterSpacing = 1.5;
const FontWeight c_logoWeight = FontWeight.w800;

// custom bottom navigation bar
const Color c_bottomNavItemColor = Color(0xffc9674d);
const Color c_whiteDark = Color(0xffe6e6e6);
const BorderRadius c_bottomNavbuttonRadius =
    BorderRadius.all(Radius.circular(18));
const EdgeInsets c_bottomNavbuttonPadding =
    EdgeInsets.symmetric(horizontal: 25, vertical: 12);
const double c_bottomNavbarHeight = 0.1;
const double c_bottomNavButtonInkRadius = 15;
